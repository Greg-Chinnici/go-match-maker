info:
  name: Glicko Report
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8080/report
  body:
    type: multipart-form
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
